[project]
name = "captain-lambda"
version = "0.1.0"
description = "Captain Nova AI agent Lambda handler"
requires-python = ">=3.12"
dependencies = [
    "aws-lambda-powertools>=3.24.0",
    "aws-xray-sdk>=2.15.0",
]

[tool.uv.sources]
shared = { workspace = true }
agent = { workspace = true }
